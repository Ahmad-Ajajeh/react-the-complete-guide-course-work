// function add(a: number, b: number) {
//   return a + b;
// }

// const result = add(2, 5);

// console.log(result);

// typescript does not run in the browser
// we need to compile code into javascript
// if we continue to use the typescript then
// compile it to javascript => we will be informed about
// the errors in this compilation phase .
